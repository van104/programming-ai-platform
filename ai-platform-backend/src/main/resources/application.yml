server:
  port: 8080

spring:
  application:
    name: ai-platform-backend
  datasource:
    url: jdbc:mysql://localhost:3306/ai_platform_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: van
    password: lrm0405
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  #czjtu_ai_platform_secret_key_2026->base64 编码
  secret: Y3pqdHVfYWlfcGxhdGZvcm1fc2VjcmV0X2tleV8yMDI2
  #1天过期（毫秒）
  expiration: 86400000

deepseek:
  api:
    # 优先使用环境变量 DEEPSEEK_API_KEY，不存在则使用下方默认值（仅开发环境）
    key: ${DEEPSEEK_API_KEY:sk-d630d2ca730f4d289c6d41649078d591}
    url: https://api.deepseek.com/v1/chat/completions
    model: deepseek-v4-flash
    mock: false
